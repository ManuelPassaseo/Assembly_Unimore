#include <stdint.h>
#include <stdio.h>

int togli_dispari(unsigned char* src, int lunghezza);


int main(void)
{
	{
	unsigned char src[] = { 0,0,0,0,0,0 };
	int lunghezza = 6;

	printf("%d\n", togli_dispari(src, lunghezza));
	for (int i = 0; i < lunghezza; i++)
		printf("%d ", src[i]);
}
	{
		unsigned char src[] = { 0,0,0,0,0,0 };
		int lunghezza = 6;

		printf("%d\n", togli_dispari(src, lunghezza));
		for (int i = 0; i < lunghezza; i++)
			printf("%d ", src[i]);
	}
	{
		unsigned char src[] = { 0,0,0,0,0,0 };
		int lunghezza = 6;

		printf("%d\n", togli_dispari(src, lunghezza));
		for (int i = 0; i < lunghezza; i++)
			printf("%d ", src[i]);
	}
	{
		unsigned char src[] = { 0,0,0,0,0,0 };
		int lunghezza = 6;

		printf("%d\n", togli_dispari(src, lunghezza));
		for (int i = 0; i < lunghezza; i++)
			printf("%d ", src[i]);
	}
	{
		unsigned char src[] = { 0,0,0,0,0,0 };
		int lunghezza = 6;

		printf("%d\n", togli_dispari(src, lunghezza));
		for (int i = 0; i < lunghezza; i++)
			printf("%d ", src[i]);
	}
	{
		unsigned char src[] = { 0,0,0,0,0,0 };
		int lunghezza = 6;

		printf("%d\n", togli_dispari(src, lunghezza));
		for (int i = 0; i < lunghezza; i++)
			printf("%d ", src[i]);
	}
	{
		unsigned char src[] = { 0,0,0,0,0,0 };
		int lunghezza = 6;

		printf("%d\n", togli_dispari(src, lunghezza));
		for (int i = 0; i < lunghezza; i++)
			printf("%d ", src[i]);
	}
	return 0;
}
